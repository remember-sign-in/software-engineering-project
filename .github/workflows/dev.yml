on:
        push:
                branches:
                        - zxj-dev
                          
jobs:                     
       restart-job:
               name: restart job
               runs-on: ubuntu-latest
               steps:
                      - uses: actions/checkout@v4
                      - name: install super
                        run: apt install supervisorctl
                      - name: ls
                        run: ls
                      - name: restart supervisor
                        run: supervisorctl restart JDQD
