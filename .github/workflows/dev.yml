on:
        push:
                branches:
                        - zxj-dev
                          
jobs:                     
       restart-job:
               name: restart job
               runs-on: ubuntu-latest
               steps:
                      - name: pwd
                        run: pwd
                      - name: enter venv
                        run: source /home/fastapi-user/software-engineering-project/.venv/bin/activate
                      - name: restart supervisor
                        run: supervisorctl restart JDQD
